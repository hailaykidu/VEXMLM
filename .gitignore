# Data and model artifacts (large, regenerable, or licence-restricted)
datasets/raw/
datasets/processed/
datasets/cache/
checkpoints/
tokenizer/artifacts/
vocabulary_expansion/artifacts/
experiment_tracking/mlruns/
experiment_tracking/runs/
legacy/*/model.safetensors
*.safetensors
*.bin
*.pt

# Logs
*.out
*.err
*.log
slurm-*.out

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# Test/tooling caches
.pytest_cache/
.ruff_cache/

# Imported datasets: copies of external data, tracked by provenance record
# (datasets/metadata/acquisition.json) rather than by content.
datasets/raw/

# Trained tokenizer artifacts and normalized corpora (regenerable, large)
tokenizer/artifacts/
logs/

# Legacy pre-VEXMLM artifacts (retained locally for provenance, not released)
legacy/

# Internal development material (retained locally, not part of the public release)
reports/internal/
scripts/internal/

# Expanded multilingual evaluation: run outputs and checkpoints (regenerable)
# results.json and the aggregated tables are kept; model artifacts are not.
# Each run copies the tokenizer out beside its results — 180 near-identical
# copies of the same few files, ~1.4GB, all reproducible from the checkpoint.
results/multilingual_evaluation/**/checkpoint-*/
results/multilingual_evaluation/**/*.safetensors
results/multilingual_evaluation/**/tokenizer.json
results/multilingual_evaluation/**/tokenizer_config.json
results/multilingual_evaluation/**/special_tokens_map.json
results/multilingual_evaluation/**/sentencepiece.bpe.model
results/multilingual_evaluation/**/config.json
results/multilingual_evaluation/**/training_args.bin
